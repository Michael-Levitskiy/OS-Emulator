/**
 * Contains the Java's main method
 * Uses Init.java to create HelloWorld and GoodbyeWorld processes
 */
public class Main {
    public static void main(String[] args) {

        OS.Startup(new Init());
    }
}